apiVersion: v1
kind: Secret
metadata:
  name: app-secrets
  namespace: agentic-empire
type: Opaque
stringData:
  # REQUIRED: Set these before deploying
  JWT_SECRET: "your-change-me-at-least-32-chars-long-secret"
  OPENAI_API_KEY: "sk-your-openai-api-key"
  POSTGRES_PASSWORD: "postgres-password"
  STRIPE_SECRET_KEY: "sk_test_..."
  SMTP_PASSWORD: "smtp-password"
  MASTER_ENCRYPTION_KEY: "your-master-encryption-key"
