# 🏗️ LucaExpress v2.0 - Architecture & Data Flow Diagrams

## System Architecture Overview

```
┌─────────────────────────────────────────────────────────────────┐
│                    LucaExpress v2.0 System                      │
└─────────────────────────────────────────────────────────────────┘

┌──────────────────────┐
│   Frontend (HTML)    │
├──────────────────────┤
│ • settings-adv.html  │  ◄────── User configures features
│ • finetuning.html    │
│ • chat.html          │
│ • dashboard.html     │
└──────────────────────┘
          │
          │ (HTTPS/WebSocket)
          ▼
┌─────────────────────────────────────────┐
│        Express.js Backend (Node)         │
├─────────────────────────────────────────┤
│  server.js (25+ API endpoints)          │
│  • /api/auto-approve/*    (3 routes)   │
│  • /api/dlp/*             (4 routes)   │
│  • /api/ollama/*          (5 routes)   │
│  • /api/agents/*          (6 routes)   │
│  • /api/finetuning/*      (5 routes)   │
│  • /api/settings/*        (2 routes)   │
└─────────────────────────────────────────┘
          │
    ┌─────┼─────┬──────────────┬──────────────┐
    │     │     │              │              │
    ▼     ▼     ▼              ▼              ▼
┌────┐ ┌──┐ ┌──────┐ ┌─────────────┐ ┌──────────┐
│DLP │ │AA│ │Agents│ │Fine-tuning  │ │Config    │
│    │ │  │ │      │ │             │ │Manager   │
└────┘ └──┘ └──────┘ └─────────────┘ └──────────┘
   │     │     │           │              │
   │     │     │           │              │
   └─────┴─────┴───────────┴──────────────┘
           │
           ▼
    ┌─────────────────┐
    │   Database      │
    │   (SQLite3)     │
    │   Users,        │
    │   Audit Logs,   │
    │   Conversations │
    └─────────────────┘

    ┌──────────────────┐
    │ Ollama (Local)   │◄──── Optional local LLM
    │ Llama3, etc      │
    └──────────────────┘
         OR
    ┌──────────────────┐
    │ OpenAI (Cloud)   │◄──── Fallback LLM
    │ GPT-4, etc       │
    └──────────────────┘
```

---

## Chat Flow with DLP & Auto-Approval

```
User Types Chat Message
       │
       ▼
   ┌─────────────────────┐
   │  Received at /chat  │
   └─────────────────────┘
       │
       ▼
   ┌─────────────────────┐
   │  1. DLP Scan        │  ◄──── dlp-engine.js
   │  ├─ Check for SSN   │
   │  ├─ Check for CC    │
   │  ├─ Check for keys  │
   │  └─ Check for PII   │
   └─────────────────────┘
       │
    ┌──┴──┐
    │ PII?│
    └──┬──┘
   Yes│ Sensitive│No
      │ Data     │
      ▼          ▼
   ┌────┐    ┌──────┐
   │Mask│    │Pass  │
   │/del│    │thru  │
   └────┘    └──────┘
      │         │
      └────┬────┘
           │
           ▼
   ┌─────────────────────┐
   │ 2. Auto-Approval    │  ◄──── Risk scoring
   │ ├─ Score action     │
   │ ├─ Check threshold  │
   │ └─ Queue if needed  │
   └─────────────────────┘
       │
    ┌──┴──┐
    │Appr?│
    └──┬──┘
   Yes│ Approved│No → Queue for approval
      │         │
      ▼         ▼
   ┌──────┐  ┌────────────┐
   │✅    │  │Waiting for │
   │Ready │  │approval    │
   └──────┘  └────────────┘
      │         │
      │         │ (human approves)
      │         ▼
      │      ┌──────┐
      │      │✅    │
      │      └──────┘
      │         │
      └────┬────┘
           │
           ▼
   ┌─────────────────────┐
   │ 3. Select LLM       │  ◄──── LLM selection
   │ ├─ Try Ollama first │
   │ │  (fast, local)    │
   │ └─ Fallback to OpenAI
   │    (if needed)      │
   └─────────────────────┘
       │
       ▼
   ┌─────────────────────┐
   │ 4. Send to LLM      │
   │    (Sanitized!)     │
   └─────────────────────┘
       │
       ▼
   ┌─────────────────────┐
   │ 5. Get Response     │
   └─────────────────────┘
       │
       ▼
   ┌─────────────────────┐
   │ 6. Log to Audit     │  ◄──── Audit trail
   │    & RAG Memory     │
   └─────────────────────┘
       │
       ▼
   ┌─────────────────────┐
   │ 7. Return Response  │
   │    to User          │
   └─────────────────────┘
       │
       ▼
   User Sees Response
```

---

## DLP Detection & Sanitization

```
┌─────────────────────────────┐
│  Input Text                 │
│  "SSN: 123-45-6789"         │
└─────────────────────────────┘
       │
       ▼
┌─────────────────────────────┐
│ DLP Pattern Detection       │
├─────────────────────────────┤
│ Regex Pattern Matching:     │
│                             │
│ ✓ SSN: \d{3}-\d{2}-\d{4}   │
│   Match: 123-45-6789        │
│                             │
│ ✗ CC, Email, Phone, etc     │
│   No match                  │
└─────────────────────────────┘
       │
       ▼
┌─────────────────────────────┐
│ Select Policy & Action      │
├─────────────────────────────┤
│ Policy: STRICT              │
│ Action: MASK                │
│                             │
│ Alternatives:               │
│ • MASK: ****-**-****        │
│ • REMOVE: (deleted)         │
│ • REDACT: [REDACTED]        │
│ • HASH: a1b2c3d4...         │
│ • ENCRYPT: (encrypted)      │
└─────────────────────────────┘
       │
       ▼
┌─────────────────────────────┐
│ Sanitized Output            │
│ "SSN: ****-**-****"         │
└─────────────────────────────┘
       │
       ▼
┌─────────────────────────────┐
│ Log Violation               │
├─────────────────────────────┤
│ timestamp: 2024-01-15...    │
│ user_id: user123            │
│ pattern: ssn                │
│ action: MASK                │
│ original: 123-45-6789       │
└─────────────────────────────┘
       │
       ▼
   Send to LLM
   (CLEAN DATA)
```

---

## Agent Autonomy Decision Loop

```
┌──────────────────────────────────────────┐
│  Agent Autonomy - Self-Direction Loop    │
└──────────────────────────────────────────┘

      Every 30 seconds:
            │
            ▼
    ┌───────────────────┐
    │ 1. Query RAG Mem  │  ◄──── Get recent context
    │    • Last 10 msg  │       from conversation
    │    • Patterns     │
    │    • Decisions    │
    └───────────────────┘
            │
            ▼
    ┌───────────────────────────────────┐
    │ 2. Compose Decision Prompt        │
    │                                   │
    │ "You are ${agentName}.            │
    │  Based on your RAG memory:        │
    │  [context...]                     │
    │                                   │
    │  What should you do next?"        │
    └───────────────────────────────────┘
            │
            ▼
    ┌───────────────────┐
    │ 3. LLM Decides    │
    │                   │
    │ Response:         │
    │ "Send report"     │
    │ Confidence: 0.92  │
    └───────────────────┘
            │
            ▼
    ┌───────────────────────────┐
    │ 4. Evaluate Decision      │
    │                           │
    │ • Check rate limit        │
    │   (5/hour max)            │
    │ • Check approval gate     │
    │   (if configured)         │
    │ • Check DLP rules         │
    │   (always enforced!)      │
    └───────────────────────────┘
            │
        ┌───┴───┐
        │Valid? │
    ┌───┴───┬───┴────┐
    │YES    │NO      │
    ▼       ▼        ▼
  ┌──┐  ┌──────┐  ┌────┐
  │✅│  │Wait  │  │Skip│
  │  │  │Queue │  │    │
  └──┘  └──────┘  └────┘
    │
    ▼
┌─────────────────┐
│ 5. Execute      │
│    Action       │
└─────────────────┘
    │
    ▼
┌──────────────────────┐
│ 6. Learn & Remember │
│                      │
│ • Log decision       │
│ • Add to RAG memory  │
│ • Calculate score    │
│ • Update patterns    │
└──────────────────────┘
    │
    ▼
  ┌──────────────┐
  │ 30 sec wait  │
  │ repeat loop  │
  └──────────────┘
```

---

## Fine-Tuning Workflow

```
┌────────────────────────────────────────┐
│  Fine-Tuning Workflow                  │
└────────────────────────────────────────┘

   1. Upload Dataset
      │
      ▼
   ┌─────────────────────┐
   │ JSON File           │
   │ ├─ instruction      │
   │ ├─ input            │
   │ └─ output           │
   │ (1,000+ examples)   │
   └─────────────────────┘
      │
      ▼
   2. Validate Dataset
      │
      ▼
   ┌─────────────────────┐
   │ • Check format      │
   │ • Check sizes       │
   │ • Check diversity   │
   │ • Remove duplicates │
   └─────────────────────┘
      │
      ▼
   3. Configure Training
      │
      ▼
   ┌─────────────────────┐
   │ Base model: Llama2  │
   │ Learning rate: 2e-5 │
   │ Epochs: 3           │
   │ Batch size: 4       │
   │ Validation: 10%     │
   └─────────────────────┘
      │
      ▼
   4. Start Training
      │
      ▼
   ┌──────────────────────────┐
   │ Training in Progress     │
   │ ├─ Epoch 1/3: 45%        │
   │ ├─ Loss: 0.234           │
   │ ├─ ETA: 2h 15m           │
   │ └─ GPU Memory: 8.2GB      │
   └──────────────────────────┘
      │
      ▼
   5. Validate Model
      │
      ▼
   ┌─────────────────────┐
   │ Metrics             │
   │ • BLEU: 0.85        │
   │ • Perplexity: 12.3  │
   │ • F1 Score: 0.92    │
   └─────────────────────┘
      │
      ▼
   6. Deploy Model
      │
    ┌─┴─────────┬──────────┐
    │           │          │
    ▼           ▼          ▼
 ┌──────┐  ┌──────────┐  ┌──────┐
 │Local │  │HuggingFace  │ONNX │
 │Down  │  │Hub         │Export
 │load  │  │            │
 └──────┘  └──────────┘  └──────┘
```

---

## Auto-Approval Decision Tree

```
┌─────────────────────────────────┐
│  Auto-Approval Decision Logic   │
└─────────────────────────────────┘

Chat Action Submitted
       │
       ▼
Calculate Risk Score (0-3)
├─ Action Type
│  ├─ Read: 0 (low risk)
│  ├─ Write: 1 (medium)
│  └─ Delete: 2 (high)
├─ Data Type
│  ├─ Public: +0
│  ├─ Internal: +0.5
│  └─ Sensitive: +1
├─ User Role
│  ├─ Admin: -1
│  ├─ Manager: 0
│  └─ User: +0.5
└─ Result: 0.0-3.0 score
       │
       ▼
Check Against Thresholds:
       │
   ┌───┼───┬─────────┐
   │   │   │         │
   ▼   ▼   ▼         ▼
  0-1  1-2  2-3    Skip Approval
   │   │   │         │
   │   │   │    ┌────┘
   │   │   └────┤
   │   └────────┤
   └────────────┤
        │
        ▼
    ┌─────────────────────┐
    │ Approval Chain      │
    │ ├─ Single: 1 vote   │
    │ ├─ Dual: 2 votes    │
    │ └─ Triple: 3 votes  │
    └─────────────────────┘
        │
        ▼
    Get Votes:
    ├─ From: Requester
    ├─ From: Manager
    ├─ From: Admin
    └─ Until threshold met
        │
    ┌───┴───┐
    │Votes? │
    └───┬───┘
    Yes│ Met│No
       │    │
       ▼    ▼
     ┌──┐ ┌────────┐
     │✅│ │⏳      │
     │GO│ │PENDING │
     └──┘ └────────┘
```

---

## System Integration Points

```
┌─────────────────────────────────────────────────────────┐
│  How Components Integrate                               │
└─────────────────────────────────────────────────────────┘

User Input
    │
    ├─────────────────┬──────────────────┬──────────────┐
    │                 │                  │              │
    ▼                 ▼                  ▼              ▼
 DLP Scan      Auto-Approval       Agent Context     Config Check
    │                 │                  │              │
    │          (Risk Scoring)    (RAG Memory)      (Settings)
    │                 │                  │              │
    └─────────────┬───┴──────────────────┴──────────────┘
                  │
                  ▼
            Decision Made
                  │
    ┌─────────────┼──────────────┐
    │             │              │
    ▼             ▼              ▼
Send to LLM   Log Event     Update Memory
    │             │              │
    │             ▼              │
    │        Audit Trail         │
    │             │              │
    └─────────────┼──────────────┘
                  │
                  ▼
            User Response
```

---

## Data Flow: From User to LLM

```
User Submits Chat
       │
       ├──────────────────────────┐
       │                          │
       ▼                          ▼
   DLP Check                  Approval Check
   (Sanitize)                 (Risk Score)
       │                          │
       ├──────────────┬───────────┘
       │              │
       ▼              ▼
  Config Check   Auto-Approval
  (Settings)     (Thresholds)
       │              │
       └──────────────┤
                      │
                      ▼
                ┌──────────────────┐
                │ All Checks Pass? │
                └────────┬─────────┘
                    ┌────┴────┐
                Yes │        │ No
                    │        │
                    ▼        ▼
              ┌───────┐   ┌──────┐
              │  LLM  │   │Queue │
              │ Send  │   │Wait  │
              └───────┘   └──────┘
                    │        │
                    │     (Human approves)
                    │        │
                    └───┬────┘
                        │
                        ▼
                   LLM Response
                        │
                        ▼
                  Log & Remember
                        │
                        ▼
                   Return to User
```

---

## Security Layers

```
┌──────────────────────────────────────┐
│  Security Protection Layers          │
└──────────────────────────────────────┘

Layer 1: Input Validation
├─ User authentication (JWT)
├─ Request validation
└─ Rate limiting

         │
         ▼
Layer 2: DLP Scanning
├─ Pattern detection
├─ Data sanitization
└─ Violation logging

         │
         ▼
Layer 3: Auto-Approval
├─ Risk scoring
├─ Approval chains
└─ Audit requirements

         │
         ▼
Layer 4: LLM Communication
├─ HTTPS encryption
├─ Prompt sanitization
└─ Response validation

         │
         ▼
Layer 5: Audit & Logging
├─ All actions logged
├─ Violation tracked
└─ Compliance ready
```

---

## Module Dependencies

```
┌─────────────────────────┐
│   server.js             │
│   (Main entry point)    │
└──────────┬──────────────┘
           │
    ┌──────┼──────┬───────────┬──────────┐
    │      │      │           │          │
    ▼      ▼      ▼           ▼          ▼
┌──────┐┌──────┐┌────────┐┌──────────┐┌────┐
│config││dlp   ││agent   ││test      ││app │
│adv   ││engine││autonomy││suite     ││    │
└──────┘└──────┘└────────┘└──────────┘└────┘
    │      │      │           │          │
    │      └──────┼───────────┼──────────┘
    │             │           │
    │             ▼           ▼
    │        ┌─────────────────┐
    │        │  Database       │
    │        │  (SQLite)       │
    │        └─────────────────┘
    │
    ▼
┌──────────────────────────┐
│ config-advanced.js       │
│ • AUTO_APPROVAL_CONFIG   │
│ • DLP_CONFIG             │
│ • OLLAMA_CONFIG          │
│ • AGENT_AUTONOMY_CONFIG  │
│ • FINETUNING_CONFIG      │
└──────────────────────────┘
```

---

## Deployment Architecture

```
┌──────────────────────────────────────┐
│  Deployment Setup                    │
└──────────────────────────────────────┘

Internet/Users
    │
    ▼
┌──────────────┐
│   Firewall   │
└──────┬───────┘
       │
       ▼
┌──────────────────────────────────┐
│   Node.js Server (Port 3000)     │
│   ├─ Express.js                  │
│   ├─ 25+ API Endpoints           │
│   └─ WebSocket Support           │
└──────┬───────────────────────────┘
       │
   ┌───┴─────┬─────────┬─────────────┐
   │         │         │             │
   ▼         ▼         ▼             ▼
┌──────┐┌──────┐┌────────┐┌──────────┐
│DLP   ││Auth  ││Logging ││Sessions  │
│Check ││      ││        ││          │
└──────┘└──────┘└────────┘└──────────┘
   │
   ├─────────────────────┬────────────┐
   │                     │            │
   ▼                     ▼            ▼
┌──────────┐      ┌─────────────┐┌────────────┐
│SQLite3   │      │Ollama       ││OpenAI API  │
│Database  │      │(Local LLM)  ││(Cloud)     │
└──────────┘      └─────────────┘└────────────┘
```

---

**End of Architecture Diagrams**

For more details, see:
- [IMPLEMENTATION_COMPLETE_V2.md](IMPLEMENTATION_COMPLETE_V2.md)
- [ADVANCED_FEATURES_GUIDE.md](ADVANCED_FEATURES_GUIDE.md)
- Individual module source files
